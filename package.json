{
  "name": "plaid-financial-insights",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install": "pnpm install",
    "build": "pnpm --filter frontend run build", 
    "start": "concurrently \"pnpm --filter backend run dev\" \"pnpm --filter frontend run start\"", 
    "postinstall": "pnpm --filter frontend install && pnpm --filter backend install" 
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^9.0.1"
  },
  "proxy": "http://localhost:5005"
}
